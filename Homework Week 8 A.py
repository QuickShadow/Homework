currentNumber = 1
maxNumber = 10

while currentNumber <= maxNumber:
        if currentNumber %2 == 0:
                print ("Found an even number: " + str(currentNumber) + ".")
        else:	
                print ("Found an odd number: " + str(currentNumber) + ".")
        currentNumber += 1

##Couldn't think of a reasonable way to implement a continue command
